{
   "ignore": ["dist/**/*", "node_modules/**/*"],
   "exec": "npm run build:dev",
   "events": {
      "restart": "kill-port  4000",
      "crash": "kill-port 4000"
   },
   "watch": ["src/**/*.ts"],
   "ext": ".ts"
}
